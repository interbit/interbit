const JOIN_TYPES = {
  PROVIDE: 'provide',
  CONSUME: 'consume',
  SEND: 'sendActionTo',
  RECEIVE: 'receiveActionFrom'
}

const ROOT_CHAIN_ALIAS = 'interbitRoot'

module.exports = {
  JOIN_TYPES,
  ROOT_CHAIN_ALIAS
}
