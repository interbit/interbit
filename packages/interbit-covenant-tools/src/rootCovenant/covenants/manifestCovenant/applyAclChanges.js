const applyAclChanges = (state, newManifest) => {
  const nextState = state

  // It hasn't been totally decided how we will manage ACL updates in the
  // manifest.

  return nextState
}

module.exports = { applyAclChanges }
