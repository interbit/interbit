// import assert from 'assert'

describe('covenant', () => {
  it('adds child chains to config', () => {})
  it('adds covenant hashes to config', () => {})
  it('adds files to the tmp layer for distribution', () => {})
  it('maps a chain to a covenant', () => {})
  it('removes a chain', () => {})
  it('updates child chains when covenant hash updated', () => {})
})
